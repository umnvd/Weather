<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/colorSurface"
    android:foreground="?android:attr/selectableItemBackground"
    android:padding="@dimen/padding_medium">

    <TextView
        android:id="@+id/itemCityNameTextView"
        android:textSize="@dimen/text_large"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="@tools:sample/cities"
        app:layout_constraintEnd_toStartOf="@id/itemCityIsCurrentImageView"
        android:layout_marginEnd="@dimen/margin_medium"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <ImageView
        android:id="@+id/itemCityIsCurrentImageView"
        android:layout_width="@dimen/current_city_icon_size"
        android:layout_height="@dimen/current_city_icon_size"
        android:visibility="visible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:src="@drawable/ic_current_city_active"
        android:background="?selectableItemBackgroundBorderless"
        tools:visibility="visible"
        android:contentDescription="@string/current_city_description"
        />

</androidx.constraintlayout.widget.ConstraintLayout>